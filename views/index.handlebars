<h1><i class="fas fa-hamburger"></i> Burgers <i class="fas fa-hamburger"></i></h1>

<div class="flexbox_card">
  <form class="create-form form">
    <h1>add a burger...</h1>
    <div class="form-group">
      <label for="ca"></label>
      <input class="search" type="text" id="ca" name="name">
      <button class="button" type="submit">Add</button>
    </div>
  </form>
</div>

<div class="best">
  <h1>your burgers:</h1>
  <ul>
    {{#each burgers}}
    {{#unless devoured}}
    <li><h2>
      {{burger_name}}
      <button class="button change-toEaten" data-id="{{id}}"><i class="fas fa-utensils"></i></button>
      </h2>
    </li>
     {{/unless}}
    {{/each}}
</div>


<div class="eaten">
  <h1>all gone!!</h1>
<ul class="all_gone">
      {{#each burgers}}
      {{#if devoured}}
      <li>
      {{burger_name}}
      <button class="button delete-burger" data-id="{{this.id}}"><i class="fas fa-trash-alt"></i></button>
      {{/if}}
      </li>
      {{/each}}
    </ul>
</div>